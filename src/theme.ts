"use client"
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#282222',
    },
    secondary: {
      main: '#9F5650',
    },
  },
});

export default theme;